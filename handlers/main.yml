---

- name: Enable and Start Iptables
  service:
    name: "{{ iptables_service_name }}"
    state: started
    enabled: yes
  tags:
    - iptables

- name: Reload Iptables Rules
  service:
    name: "{{ iptables_service_name }}"
    state: restarted
  tags:
    - iptables
