---
# tasks file for iptables (Ubuntu specific)

- name: Install iptables packages
  apt:
    name: "{{ item }}"
    state: present
    update_cache: yes
    cache_valid_time: 86400
  with_items:
    - "{{ iptables_packages }}"
